设计一个面向企业团队的高端深色任务管理后台，桌面端横向布局，参考视口比例 16:9，建议画布 1440×810 或 1920×1080。整体呈现未来感、专业感、克制的黑色玻璃拟态风格，结合空间纵深、半透明面板、精细描边、柔和高光和少量荧光绿色作为核心视觉焦点。



产品定位：

企业级任务管理、项目协作和智能分析控制台。界面用于查看任务统计、任务列表、项目进度、时间线和 AI 智能建议。视觉应高级、理性、安静、精密，像成熟的生产力工具，不像游戏官网、营销落地页或科幻概念海报。



一、整体布局

采用三栏桌面应用结构：

\1. 左侧固定导航栏，宽度约占页面 14%。

\2. 中央主工作区，宽度约占页面 66%。

\3. 右侧智能详情栏，宽度约占页面 20%。

\4. 页面四周保留约 16–24px 安全边距。

\5. 各区域之间使用 12–18px 间距。

\6. 页面背景为接近纯黑的 #050606，带非常微弱的绿色环境光和低对比噪点。

\7. 主体内容密集但层级清晰，避免大面积空白。



二、左侧导航栏

设计一个独立的竖向黑色玻璃面板：

\- 高度接近整屏，圆角约 20–24px。

\- 背景为 #0A0C0C，透明度约 92%。

\- 边缘使用 1px 半透明灰白描边。

\- 外侧有极轻微黑色投影和顶部冷白高光。

\- 顶部展示简洁品牌标志“WB”和品牌名称“WenXiBuddy”。

\- 品牌标志使用紧凑白色无衬线字母，不添加彩色渐变。

\- 导航项采用图标加文字形式，单行高度约 44–50px。

\- 导航图标为细线风格，尺寸约 18px。

\- 菜单包含：任务管理、项目总览、文件归档、日程管理、团队协作、智能分析、知识库、设置中心。

\- 当前选中项为“任务管理”，使用半透明灰色胶囊背景、左侧绿色状态图标、右侧箭头。

\- 非选中项使用中灰文字和低亮度线性图标。

\- 导航下半部分增加分隔线和“我的工作区”标题。

\- 工作区条目展示“产品研发中心”，右侧有展开箭头。

\- 底部展示用户头像、姓名 Brandon、绿色在线状态和更多操作按钮。



三、顶部工具栏

中央内容区顶部设置一条轻量工具栏：

\- 左侧显示页面标题“任务管理”。

\- 标题使用 24–28px 半粗体白色字体。

\- 标题下方显示一句低对比辅助说明：“高效规划 · 智能协同 · 结果驱动”。

\- 中部放置横向搜索框，宽度约 320–380px。

\- 搜索框为深黑半透明胶囊形，包含搜索图标、占位文字“搜索任务、项目或文件…”以及快捷键标识。

\- 右侧依次放置通知、消息图标。

\- 最右侧为“新增任务”按钮，使用加号图标、白色文字和深色玻璃背景。

\- 按钮右侧带独立下拉箭头区域。

\- 所有按钮高度约 38–42px，圆角约 12–16px。



四、任务统计卡片

在顶部工具栏下方放置四张横向统计卡片：

\- 卡片依次为：今日待办、进行中、已完成、逾期任务。

\- 前三张卡片宽度较大，第四张略窄。

\- 背景为深黑玻璃材质，具有轻微透明度。

\- 卡片圆角约 18–22px。

\- 边缘使用柔和银灰描边。

\- 卡片底部或右下角带抽象银白流线高光，像磨砂玻璃内的光带。

\- 每张卡片左侧显示小标题和大号数字。

\- 数字分别为 12、28、56、3，字号约 32–38px。

\- 数字旁显示“项任务”或“项任务”的小型单位。

\- 下方显示“较昨日 ↑20%”“较昨日 ↑8%”“较昨日 ↑15%”“较昨日 ↓40%”。

\- 正向变化用低饱和绿色，逾期变化和告警使用红色。

\- 卡片右上角放置圆形图标容器，分别使用剪贴板、趋势线、完成勾选、告警图标。

\- 圆形图标容器为半透明黑色，带轻微内阴影和边缘高光。



五、任务看板区域

统计卡片下方为“任务看板”：

\- 标题下放置分段筛选器：全部任务、我负责的、我参与的。

\- 当前选中“全部任务”，使用浅灰透明胶囊背景。

\- 右侧放置状态筛选下拉菜单。

\- 区域右上角放置“筛选”“排序”和视图切换图标按钮。

\- 左侧任务列表占看板约 28%–32%。

\- 任务按折叠分组展示，例如：需求评审、产品设计、开发实现。

\- 每个分组标题带彩色状态点、数量和展开箭头。

\- 任务行包含编号、任务名称、状态标签、时间或截止日期。

\- 任务行高度紧凑，约 34–40px。

\- 当前任务使用微弱绿色背景或绿色左侧标识。

\- 所有文字采用白色、灰色和极少量绿色/橙色状态色。



六、中央核心三维项目卡片

任务列表右侧展示界面的视觉核心：

\- 使用一组沿水平方向排列、带透视关系的半透明玻璃卡片。

\- 卡片像文件夹、项目页或任务面板，在三维空间中依次向右后方排列。

\- 每张卡片略微旋转，形成层叠、悬浮、纵深和运动感。

\- 非当前卡片为透明磨砂玻璃，颜色接近银白、烟灰和黑色。

\- 卡片边缘有细窄白色高光，内部包含若隐若现的表格、文本、头像和状态图标。

\- 当前卡片位于中间，使用高亮荧光绿色 #00E875 至 #00FF88。

\- 绿色卡片具有轻微自发光效果，但不得产生大片霓虹光晕。

\- 当前卡片底部显示“进行中”。

\- 左前方放置一张更大的项目概览卡，略微倾斜并悬浮于其他卡片之上。

\- 概览卡显示“项目文档”“2025-02”“87% 完成度”以及右上角箭头。

\- 卡片背景为半透明烟灰玻璃，下方带柔和白色反射光。

\- 三维卡组后方保持纯黑背景，以突出透明层次。

\- 整体应像可交互的项目文件轮播，而不是普通二维卡片列表。



七、底部项目时间线

中央工作区底部放置横向甘特图/时间线面板：

\- 面板横跨主内容区。

\- 标题为“项目时间线”，旁边显示日历图标和“2025年5月”。

\- 顶部显示连续日期刻度。

\- 当前日期使用绿色圆形标记和贯穿面板的细绿色竖线。

\- 左侧列出任务名称，如需求评审、产品设计、开发实现、测试验收。

\- 右侧使用灰色和绿色胶囊条表示任务周期。

\- 当前进行中的任务条使用绿色。

\- 其他任务条使用深灰或银灰色。

\- 每个时间条包含任务名称和日期范围。

\- 面板背景为 #0D0F0F，圆角约 12–16px，边框低对比。

\- 信息密度较高，但日期和任务条不能重叠。



八、右侧智能详情栏

设计一个独立的竖向智能详情面板：

\- 与左侧导航栏形成视觉平衡。

\- 使用深色玻璃背景、20px 左右圆角、银灰描边和顶部高光。

\- 顶部标题为“智能详情”，左侧使用闪光/AI 图标，右侧使用搜索图标。

\- 显示任务编号“WXB-2025-001”。

\- 主标题为“需求评审会”，旁边显示绿色“高优先级”标签。

\- 下方显示简短任务描述。

\- 使用键值列表展示负责人、所属项目、截止时间、当前状态、优先级和标签。

\- 负责人包含小型圆形头像及 Brandon 名称。

\- 当前状态使用绿色状态点和“进行中”。

\- 优先级使用红色小圆点和“高”。

\- 标签使用多个小型灰色胶囊标签。

\- 中下部放置“AI 助手建议”卡片。

\- 建议卡片使用更浅一点的黑灰背景、低对比描边和微弱内阴影。

\- 卡片内包含两到三条项目建议，以紧凑项目符号排列。

\- 卡片底部放置“查看建议详情”按钮。

\- 面板底部固定操作栏，包含“编辑任务”“完成任务”和更多菜单。

\- “完成任务”为绿色描边按钮，带勾选图标。

\- 其他按钮保持低亮度深色风格。



九、视觉语言

\- 核心风格：黑色玻璃拟态、未来企业软件、空间层叠、克制荧光绿。

\- 主背景：#050606。

\- 次级面板：#0A0C0C、#0E1010。

\- 高层面板：rgba(25, 28, 28, 0.72)。

\- 主文字：#F2F4F3。

\- 次文字：#A1A7A4。

\- 弱文字：#666D69。

\- 主强调色：#00E875。

\- 高亮强调色：#16FF8A。

\- 警告色：#FF9F43。

\- 危险色：#FF4D64。

\- 描边：rgba(255,255,255,0.10)。

\- 高光描边：rgba(255,255,255,0.22)。

\- 避免大面积绿色，只在当前状态、关键指标和主操作中使用。

\- 不使用蓝紫渐变，不使用彩色光斑，不使用大面积霓虹背景。

\- 面板投影要轻，主要依靠边框、透明度和局部反射建立层次。



十、字体与图标

\- 使用现代中性无衬线字体，例如 Inter、SF Pro、HarmonyOS Sans 或思源黑体。

\- 页面标题 24–28px，字重 600。

\- 模块标题 16–18px，字重 600。

\- 卡片数字 32–38px，字重 500。

\- 正文 13–14px。

\- 辅助文字 11–12px。

\- 字间距保持 0，不使用负字距。

\- 图标采用 Lucide 风格细线图标，线宽统一。

\- 图标以白色、灰色和状态色呈现，不使用拟物彩色图标。



十一、材质与光影

\- 所有主面板使用半透明深色玻璃材质。

\- 面板顶部和左上角有非常轻微的冷白高光。

\- 卡片内部可加入低强度径向反射，但不能出现明显渐变球。

\- 三维项目卡片使用真实透视、轻微景深和边缘反射。

\- 荧光绿色当前卡片是唯一强视觉焦点。

\- 黑色区域保留细节，不可压成完全无层次的纯黑。

\- 保证文字和背景有足够对比度。

\- 光影方向统一，主要从左上方和绿色卡片位置产生局部反射。



十二、交互状态

\- 导航项悬停时背景略微变亮。

\- 卡片悬停时提升 2–4px，边框亮度增强。

\- 当前三维项目卡片保持绿色选中状态。

\- 任务行悬停显示操作按钮。

\- 分组支持折叠和展开。

\- 搜索、筛选、排序和视图切换均为真实可用控件。

\- 点击任务后更新右侧智能详情。

\- 点击三维卡片后将其移动到视觉中心。

\- 时间线支持横向滚动。

\- 所有动效控制在 160–260ms，使用平滑缓动，不使用夸张弹跳。

\- 页面在 1440px 以上保持三栏结构；较窄桌面隐藏右侧详情栏并通过抽屉打开。



最终效果：

一个高级、精密、充满空间纵深的企业任务管理控制台。界面以黑色玻璃面板为基础，以银灰反射建立层次，以单一荧光绿色突出当前项目和关键状态。整体视觉接近高端操作系统工具、专业项目管理软件与轻量未来工业设计的结合，信息密集但秩序清楚，适合长期高频使用。

不要浅色背景，不要白色仪表盘，不要传统蓝色 SaaS 风格，不要紫色渐变，不要蓝紫霓虹，不要彩虹色，不要大面积绿色，不要发光圆球，不要光斑和 bokeh，不要营销落地页，不要巨型标题，不要英雄区，不要插画，不要卡通图标，不要游戏 HUD，不要赛博朋克城市背景，不要夸张霓虹灯，不要粗重描边，不要高饱和配色，不要过度模糊，不要过大圆角，不要胶囊化所有组件，不要卡片嵌套卡片，不要空旷布局，不要低信息密度，不要文字重叠，不要控件尺寸不一致，不要把三维卡组做成普通二维列表，不要牺牲可读性换取氛围感。

高端黑色玻璃拟态企业任务管理后台，三栏桌面布局，固定左侧导航，中央任务统计卡、分组任务列表、三维透明项目文件轮播、底部甘特时间线，右侧 AI 智能详情面板；深黑背景，银灰玻璃边框，局部冷白反射，单一荧光绿色当前项目卡，细线图标，现代无衬线字体，高信息密度，真实产品级 UI，空间纵深，克制未来感，精密、专业、安静、可交互，16:9 desktop dashboard。