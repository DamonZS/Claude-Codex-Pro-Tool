# Multica 独立评论资源

## 背景
上游 Multica 将评论作为独立资源，支持线程、作者、解析状态和来源任务。工作区需要提供同样的本地可查看与编辑能力。

## 目标
- 增加独立 `comments` 集合，不把评论伪装成 Issue 字段。
- 支持正文、类型、父评论、作者、解析状态和来源任务字段，并使用 revision CAS。
- 校验评论归属已存在任务，拒绝空正文、NUL、超长正文和不可创作的 system 类型。

## 非目标
- 本阶段不实现附件、通知、WebSocket、@agent 调度或远端 HTTP API。
